# Backend environment example - copy to .env and fill with real values.
# Keep the real .env out of version control. Do NOT commit secrets.

# Your Supabase project URL (from Supabase project settings)
SUPABASE_URL=https://xyzcompany.supabase.co

# Supabase service role key (server-only secret). Keep this in your backend env or secret store.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example

# Optional: public anon key for server-side use (not recommended for elevated operations)
SUPABASE_ANON_KEY=public-anon-key-example

# HMAC authentication key for internal API routes (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
INTERNAL_API_KEY=your-secret-key-here

# Server port
PORT=3000
