// Page removed: route disabled
// To fully remove the route, delete this file. For now we return a notFound response
// so the path returns a 404 if requested.

import { notFound } from 'next/navigation';

export default function SubjectGroupsPage() {
  // route intentionally removed — return 404
  notFound();
}
